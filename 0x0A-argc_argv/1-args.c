#include <stdio.h>
/**
 * main - write a program that prints the number of arguments pased into it
 * @args: this is argument for count
 * @argv: this is argument for vector
 *
 * Return: return 0
 */
int main(int args, char *argv[])
{
	printf("d\n", args - 1);
	(void)argv;
	return (0);
}
